// export const generateMockRankings = (count: number) => {
//   return Array.from({ length: count }, (_, i) => ({
//     id: `mock-team-${i + 1}`,
//     name: `Mock Team ${i + 1} - Project ${String.fromCharCode(65 + (i % 26))}`,
//     totalReward: Math.floor(Math.random() * 100) + 1, // Random score between 1000 - 51000
//     imageCover: null,
//     rank: 0, // Will be recalculated after merge
//   }));
// };
